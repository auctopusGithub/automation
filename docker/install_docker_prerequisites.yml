- name: Install Docker Prerequisites
  block:      
    - name: Install prerequisites
      ansible.builtin.apt:
        name:
          - curl
          - apt-transport-https
          - ca-certificates
          - gnupg
          - lsb-release
          - software-properties-common
      become: yes